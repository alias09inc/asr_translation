[project]
name = "asr-translatation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "transformers>=4.57.3",
]

[dependency-groups]
dev = [
    "poethepoet>=0.38.0",
]
